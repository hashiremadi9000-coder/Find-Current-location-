# Find-Current-location-
 A simple website to find your **current location**, including **latitude, longitude, city, and country**. Built using **HTML, CSS, and JavaScript**.  It works on both **desktop and mobile browsers**, and even if the user denies location permission, it fetches approximate location using **IP-based fallback**.
